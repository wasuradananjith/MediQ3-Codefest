<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_first"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.wasuradananjith.mediq.First"
    android:background="@drawable/homescreen">

    <Button
        android:text="Log Out"
        android:layout_width="100dp"
        android:layout_height="41dp"
        android:id="@+id/btnLogout"
        android:background="@drawable/rounded_button"
        android:textColor="@android:color/holo_red_dark"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:id="@+id/requestButton"
        android:layout_width="250dp"
        android:layout_height="41dp"
        android:layout_gravity="center"
        android:layout_marginBottom="31dp"
        android:textAllCaps="false"
        android:text="REQUEST"
        android:textColor="@color/colorPrimary"
        android:background="@drawable/rounded_button"
        android:layout_above="@+id/currentNoButton"
        android:layout_centerHorizontal="true" />

    <TextView
        android:text="Need Help?"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btnLogout"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="32dp"
        android:id="@+id/help"
        android:textColor="@color/colorPrimary"
        android:textStyle="bold" />

    <Button
        android:id="@+id/currentNoButton"
        android:layout_height="41dp"
        android:textColor="@color/colorPrimary"
        android:layout_width="250dp"
        android:layout_marginBottom="31dp"
        android:background="@drawable/rounded_button"
        android:text="CURRENT NUMBER"
        android:layout_above="@+id/help"
        android:layout_alignStart="@+id/requestButton" />

</RelativeLayout>
